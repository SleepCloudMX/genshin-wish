# scripts/analysis/ — 开发分析脚本

在此目录下的脚本用于验证模型、对比算法性能。输出统一放到 `output/analysis/` 对应子目录。

约定与外围 `scripts/CLAUDE.md` 相同：只输出图表 + data.json，不嵌入分析文本。

## 修改绘图时的约束

- `task*.py` 跑完整的计算+绘图极耗时。修改绘图代码时，**优先使用 `--plot-only`**，只从已有 `data.json` 重新生成图表，不重跑数值计算。
- 只有需要新数据（如修改了计算逻辑、增删方法）时才可以重跑，且**重跑前必须征求同意**。
